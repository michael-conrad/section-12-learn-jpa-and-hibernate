CREATE TABLE course
(
    id     BIGINT,
    name   VARCHAR(255) NOT NULL,
    author VARCHAR(255) NOT NULL,
    primary key (id)
);

CREATE TABLE course_table
(
    id     BIGINT,
    name   VARCHAR(255) NOT NULL,
    author VARCHAR(255) NOT NULL,
    primary key (id)
);
